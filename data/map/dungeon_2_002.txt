<?xml version = "1.0" encoding = "UTF-8" ?>
<map>
    <information>
        <name>dungeon_2_002</name>
        <width>40</width>
        <height>40</height>
        <biome>dungeon_002_biome.txt</biome>
        <map_previous>50</map_previous>
        <map_previous_portal>1</map_previous_portal>
        <map_next>52</map_next>
        <map_next_portal>1</map_next_portal>
    </information>

﻿    <generation>
       <generate>1</generate>
       <seed>0</seed>
       <algorithm>3</algorithm>
       <wall_width>1</wall_width>
    </generation>


    <player>
        # This is used when not warping from a prior map
        <player_start_tile>0842</player_start_tile>
        <player_start_rotation>1.5708</player_start_rotation>
    </player>

    <portals>
        # These are used when warping from a prior map
        # Portal number, Tile location, player face direction
        # These are exit portals
        <portal>1 0642 0.0000</portal> #Town 1 exit
    </portals>
    
    <events>
        # Tile location, type, data 1, data 2, data 3
        # Type 1 (Warp to map) : tile, event type, map number, portal number, NA
        # Type 2 (Set entity state, index from 1) : tile, event type, entity tile, state, NA
        # Type 3 (Toggle entity state, index from 1) : tile, event type, entity tile, state 1, state 2

        # up to town
        <event>0541 1 00 2 0</event> # Town 1 tower 1 entrance

    </events>

    # roomTypeNone         =  0, // None / empty
    # roomTypeCell         =  1, // Prison Cell
    # roomTypeSecret       =  2, // Secret room
    # roomTypeStairwell    =  3, // Stairwell
    # roomTypeStore        =  4, // Store
    # roomTypeBlacksmith   =  5, // Blacksmith
    # roomTypeCanteen      =  6, // Canteen
    # roomTypeStorage      =  7, // Storage room
    # roomTypeLibrary      =  8, // Library
    # roomTypeAlchemyLab   =  9, // Alchemy Lab
    # roomTypeLaboratory   = 10, // Laboratory
    # roomTypeTorture      = 11  // Torture Chamber
    <rooms>
        #Tile (any within the room), type, ignore room number (use for the interconnecting route, -1 for none)
    </rooms>

    <npc_mobs>
        #npc name, scale max, scale min, prevalence
        <#npc_mob>forest_monster 1.0 1.0 1</npc_mob> # forest_monster
        <#npc_mob>golem          1.0 1.0 2</npc_mob> # golem
        <#npc_mob>dragon         1.0 1.0 3</npc_mob> # dragon
        <#npc_mob>bat            0.5 1.0 4</npc_mob> # bat
        <npc_mob>spider         0.5 1.0 5</npc_mob> # spider
        <#npc_mob>werewolf       0.5 1.0 6</npc_mob> # werewolf
        <#npc_mob>skeleton       0.5 1.0 1</npc_mob> # skeleton
    </npc_mobs>

    <npcs>
        #tile number, npc name, index (0 == random), scale, y-rotation
    </npcs>

    <debris_objects>
        #object name, scale max, scale min, prevalence, obstacle
        <#debris>rock_small 0.5 1.0  20 0</debris> # rock small
        <#debris>dead_tree  0.5 1.0  30 1</debris> # dead tree
        <debris>bones      0.5 1.0  40 0</debris> # bones
        <#debris>gib        0.5 1.0  42 0</debris> # gibs
    </debris_objects>

    <objects>
        #tile number, object name, scale, y-rotation, obstacle, trigger tile
        
    </objects>


</map>
