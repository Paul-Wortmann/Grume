<?xml version = "1.0" encoding = "UTF-8" ?>
<map>
    <information>
        <name>dungeon_1_002</name>
        <width>100</width>
        <height>100</height>
        <biome>dungeon_001_biome.txt</biome>
    </information>
    <generation>
       <generate>1</generate>
       <seed>0</seed>
       <algorithm>3</algorithm>
       <wall_width>1</wall_width>
    </generation>

    <player>
        # This is used when not warping from a prior map
        <player_start_tile>0842</player_start_tile>
        <player_start_rotation>1.5708</player_start_rotation>
    </player>

    <portals>
        # These are used when warping from a prior map
        # Portal number, Tile location, player face direction
        # These are exit portals
    </portals>
    
    <events>
        # Tile location, type, data 1, data 2, data 3
        # Type 1 (Warp to map) : tile, event type, map number, portal number, NA
        # Type 2 (Set entity state, index from 1) : tile, event type, entity tile, state, NA
        # Type 3 (Toggle entity state, index from 1) : tile, event type, entity tile, state 1, state 2
    </events>
    <rooms>
        #Tile (any within the room), type, ignore room number (use for the interconnecting route, -1 for none)
    </rooms>

    <debris_objects>
        #object number, scale max, scale min, prevalence, obstacle
        <debris>6 0.5 1.0 15 0</debris> # bones
    </debris_objects>

    <objects>
        #tile number, object number, scale, y-rotation, obstacle
    </objects>

    <npc_mobs>
        #npc number, scale max, scale min, prevalence
        <npc_mob>2 0.5 1.0 5</npc_mob> # bat
    </npc_mobs>

    <npcs>
        #tile number, npc number, index (0 == random), scale, y-rotation
        # town central
    </npcs>

</map>
